<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="numpad">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">48sp</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="keypad" parent="numpad">
        <item name="android:textColor">@color/colorAccent2</item>
        <item name="android:textSize">32sp</item>
        <item name="android:background">@color/colorPrimarySec</item>
    </style>

    <style name="equation">
        <item name="android:textColor">@color/colorAccent3</item>
        <item name="android:textAlignment">textEnd</item>
        <item name="android:textSize">48sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:textIsSelectable">true</item>
        <item name="android:layout_gravity">right</item>
    </style>

    <style name="history" parent="equation">
        <item name="android:textSize">32sp</item>
    </style>

</resources>
